import { DocumentsPage } from '@/pages/documents'

/** Client portal documents — reuses the shared documents page with client scoping via RLS. */
export function ClientDocumentsPage() {
  return <DocumentsPage />
}
